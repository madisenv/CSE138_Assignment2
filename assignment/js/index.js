"use strict";

let app = {};


app.data = {    
    data: function() {
        return {
            // Complete.
        };
    },
    methods: {
        // Complete.
    }
};

app.vue = Vue.createApp(app.data).mount("#app");
app.vue.recompute();

